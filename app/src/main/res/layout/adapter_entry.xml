<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <import type="android.view.View" />
        <variable name="entry" type="com.tagreader.model.Entry" />
    </data>
    <android.support.constraint.ConstraintLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="4dp"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:layout_marginBottom="12dp">
        <TextView
            android:id="@+id/date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:lines="1"
            android:text="@{entry.date}"
            android:textSize="12dp"
            tools:text="2018-03-27"
            />
        <TextView
            android:id="@+id/counters"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:lines="1"
            android:text="@{@string/entry_couters(entry.voteCount, entry.commentCount)}"
            android:textSize="12dp"
            tools:text="Votes 5 / Comments 12"
            />
        <ImageView
            android:id="@+id/preview"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:layout_marginTop="8dp"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/date"
            tools:src="@drawable/ic_launcher_background"
            android:scaleType="center"
            android:visibility="@{entry.hasImage ? View.VISIBLE : View.GONE}"
            app:src="@{entry.image}"
            />
        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toBottomOf="@id/preview"
            android:layout_marginTop="8dp"
            android:lines="1"
            android:ellipsize="end"
            android:text="@{entry.header}"
            android:textSize="16dp"
            android:textColor="#000"
            tools:text="Title"
            />
        <TextView
            android:id="@+id/tags"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toBottomOf="@id/title"
            android:layout_marginTop="4dp"
            android:lines="1"
            android:text="@{entry.tags}"
            android:visibility="@{entry.hasTags ? View.VISIBLE : View.GONE}"
            tools:text="#tag"
            />
        <TextView
            android:id="@+id/description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tags"
            android:layout_marginTop="4dp"
            android:text="@{entry.content}"
            android:textColor="#000"
            android:textSize="12dp"
            tools:text="Description"
            />
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="1dp"
            app:layout_constraintTop_toBottomOf="@id/description"
            android:layout_marginTop="8dp"
            android:background="#aaa"
            />
    </android.support.constraint.ConstraintLayout>
</layout>